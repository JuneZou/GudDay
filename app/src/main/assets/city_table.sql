CREATE TABLE IF NOT EXISTS city_db (
    id integer primary key,
    cityName varchar,
    cityCode varchar,
    cityProvince varchar,
    cityIndex integer
);
create table if not exists city_history (

    id integer primary key,
    name varchar
)


